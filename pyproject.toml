[project]
name = "aep-quickstart"
version = "0.1.0"
description = "AEP Trust Engine quickstart examples — AI agent safety in 5 minutes"
requires-python = ">=3.12"
dependencies = [
    "aceteam-aep[all]>=0.4.0",
    "openai>=1.65.0",
    "anthropic>=0.40.0",
    "python-dotenv>=1.0",
]

[dependency-groups]
dev = []
