# Safety ON — all detectors active
default_action: flag
block_on: [high]
flag_on: [medium, low]

detectors:
  pii:
    enabled: true
    action: block
  agent_threat:
    enabled: true
    action: block
  cost_anomaly:
    enabled: true
    action: flag
    multiplier: 5
